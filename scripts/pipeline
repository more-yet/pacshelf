#!/usr/bin/env bash

set -euo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

[[ $# -eq 1 ]] || {
    printf 'Usage: %s <package>\n' "$(basename "$0")" >&2
    exit 2
}

package_name="$1"

"${root}/scripts/build" "${package_name}"
"${root}/scripts/validate" "${package_name}"

printf '\n==> Pipeline complete: %s\n' "${package_name}"
